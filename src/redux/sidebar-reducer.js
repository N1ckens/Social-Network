let initialState = {
    friends: [
        {id: 1, name: 'Serega'},
        {id: 2, name: 'Alik'},
        {id: 3, name: 'Tusik'}
    ]
};

const SidebarReducer = (state = initialState, action) => {
    return state;
};
export default SidebarReducer;